# A Scheme Interpreter in Python

This package implements a very simple Scheme Interpreter in Python.
Example usage:
```
import scheme
context = scheme.SchemeContext()
print(context.evaluate('(+ 1 1)'))
```

